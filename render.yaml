services:
  - type: web
    name: fancyperfume-api
    env: node
    rootDir: back
    # Use the lockfile for a clean, deterministic install on Render
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
